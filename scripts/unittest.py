import unittest

class ArithTest(unittest.TestCase):
    def run test (self):
        self.failIf(index(), "Error")
        self.failUnless(index(), "Another Error")
        self.failIfEqual(index(), "None")
        self.failUnlessEqual(index(), "Error again")		